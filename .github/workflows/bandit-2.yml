name: bandit2
on: [pull_request]
jobs:
  bandit2:
    runs-on: ubuntu-latest
    steps:
      - name: security test
      - uses: mdegis/bandit-action@v1  # v3
        with:
          path: src
          GITHUB_TOKEN: ${{ secrets.MAIN_GITHUB_TOKEN }}
          path: "."
          level: high
          confidence: high
          exit_zero: true



